[phases.setup]
aptPkgs = ["fonts-noto", "fonts-dejavu-core", "fonts-dejavu-extra"]

[phases.install]
cmds = ["pip install -r requirements.txt", "playwright install --with-deps chromium"]

[start]
cmd = "python bot.py"
